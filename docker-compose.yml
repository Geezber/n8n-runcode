version: '3.8'

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n:latest
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_HOST=0.0.0.0
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - NODE_ENV=production
      
      # Database: Supabase Configuration
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_DATABASE=postgres
      - DB_POSTGRESDB_HOST=aws-1-eu-west-1.pooler.supabase.com
      - DB_POSTGRESDB_PORT=6543
      - DB_POSTGRESDB_USER=postgres.paboqwemddfqarqagqpi
      - DB_POSTGRESDB_PASSWORD=ZXFiY6P#QqM8L+_
      - DB_POSTGRESDB_SSL_REJECT_UNAUTHORIZED=false
      
      # Security (Required to keep your workflows safe)
      - N8N_ENCRYPTION_KEY=geezber_clawcloud_2026
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data:
